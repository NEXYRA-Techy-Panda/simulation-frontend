// Public VPS deployment configuration. The API path prefix is part of the
// fixed base URL; adapters append contract routes such as /api/v1/health.
export const SIMULATION_BACKEND_URL =
  "https://git-pipeline.metatronhost.in/sim";
