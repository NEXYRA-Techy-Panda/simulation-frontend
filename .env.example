# Public backend origin only (no credentials in NEXT_PUBLIC variables).
# Use the contract's full route paths (e.g. /api/v1/health) separately to
# avoid duplicated prefixes. No backend request is made in F2-A.
NEXT_PUBLIC_SIMULATION_BACKEND_URL=http://localhost:4000
