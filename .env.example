# Optional local override. Use an origin/base URL only; adapters append the
# full contract routes such as /api/v1/health themselves.
# NEXT_PUBLIC_SIMULATION_BACKEND_URL=http://localhost:4000
#
# When unset, the app uses the public deployment base:
# https://git-pipeline.metatronhost.in/sim
